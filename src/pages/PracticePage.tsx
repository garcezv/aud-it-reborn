// Practice Page: Same as Protocol Setup but creates practice profiles
import ProtocolSetupPage from "./ProtocolSetupPage";

const PracticePage = () => {
  return <ProtocolSetupPage isPractice />;
};

export default PracticePage;
